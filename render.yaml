services:
  - type: web
    name: sleepwealth-paper-lab
    runtime: python
    plan: free
    region: virginia
    branch: main
    buildCommand: pip install -e .
    startCommand: python -m backend.server --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeployTrigger: checksPass
